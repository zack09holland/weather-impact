const styles = theme => ({
    root: {
        display: "flex",
    	  height: "64px",
      },
      menuButton: {
        marginRight: "2em",
      },
      title: {
        flexGrow: 1,
      },
});
export default styles;